{
  "name": "@rspack-repro/app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm-run-all build:* --sequential --print-name",
    "build:rspack": "npx rspack build --mode=production",
    "build:webpack": "WEBPACK=1 pnpm webpack -c ./config.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@rspack-repro/lib": "*",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@rspack-repro/build-tools": "*",
    "npm-run-all": "^4.1.5"
  }
}
